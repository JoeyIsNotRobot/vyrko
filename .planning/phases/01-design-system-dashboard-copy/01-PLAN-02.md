---
phase: 01-design-system-dashboard-copy
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - resources/views/layouts/app.blade.php
  - resources/css/app.css
  - app/Http/Controllers/DashboardController.php
  - resources/views/dashboard/index.blade.php
  - resources/views/welcome.blade.php
  - lang/pt_BR/messages.php
  - resources/views/account/index.blade.php
  - resources/views/resumes/templates.blade.php
autonomous: true
requirements:
  - DESIGN-02
  - DASH-01
  - DASH-02
  - COPY-01
  - COPY-02

must_haves:
  truths:
    - "Em páginas com pouco conteúdo, o footer aparece no rodapé da viewport, não no meio da página"
    - "O dashboard exibe exatamente 3 cards: Vagas analisadas, Currículos gerados, Completude do inventário"
    - "Quando completeness < 80%, o card de completude exibe borda amber e link 'Complete seu perfil →'"
    - "A landing exibe 'Seu currículo certo, para a vaga certa.' como h1"
    - "A página /career exibe título 'Seu inventário de carreira'"
    - "A página /account exibe título 'Sua conta'"
    - "A página de templates exibe heading 'Qual currículo enviar para esta vaga?'"
    - "Empty states do dashboard e listas exibem strings orientadoras corretas"
  artifacts:
    - path: "resources/views/layouts/app.blade.php"
      provides: "app-shell com flex flex-col; main com flex-1"
      contains: "flex-1"
    - path: "resources/css/app.css"
      provides: ".app-shell com display: flex; flex-direction: column"
      contains: "flex-direction: column"
    - path: "app/Http/Controllers/DashboardController.php"
      provides: "$completeness integer calculado a partir de $missingInventory"
      contains: "completeness"
    - path: "resources/views/dashboard/index.blade.php"
      provides: "grid de 3 cards sem summary-grid"
      contains: "grid grid-cols-1 sm:grid-cols-3 gap-6"
    - path: "resources/views/resumes/templates.blade.php"
      provides: "heading 'Qual currículo enviar para esta vaga?'"
      contains: "Qual currículo enviar para esta vaga?"
  key_links:
    - from: "DashboardController"
      to: "dashboard/index.blade.php"
      via: "variável $completeness passada ao view()"
      pattern: "'completeness' => \\$completeness"
    - from: "dashboard/index.blade.php card de completude"
      to: "route('career.index')"
      via: "link condicional quando $completeness < 80"
      pattern: "career\\.index"
    - from: "app.blade.php main"
      to: ".app-shell flex container"
      via: "classe flex-1 em main + display:flex no .app-shell"
      pattern: "flex-1"
---

<objective>
Três mudanças paralelas sobre a fundação de cores do Wave 1:

1. DESIGN-02 — Footer fixo: adiciona flex flex-col ao .app-shell e flex-1 ao main em app.blade.php + app.css, fazendo o footer grudar no rodapé independente do tamanho do conteúdo.

2. DASH-01 + DASH-02 — Dashboard 3 cards: DashboardController computa $completeness (0–100); dashboard/index.blade.php substitui o section.summary-grid de 4 cards por um grid de 3 colunas com os cards especificados na UI-SPEC, incluindo card de completude inline com CTA condicional.

3. COPY-01 + COPY-02 — Copy strings: headline da landing, títulos do inventário e conta, heading/subtitle do dashboard, heading da página de templates e empty states de dashboard e listas atualizados para os textos da UI-SPEC.

Purpose: Entrega as três áreas de produto visíveis ao usuário que são independentes entre si mas todas dependem do sistema de cores do Wave 1.

Output: Layout com footer fixo; dashboard 3 cards; textos PT-BR corretos nas 4 telas principais + templates + empty states.
</objective>

<execution_context>
@/home/joey/.claude/get-shit-done/workflows/execute-plan.md
@/home/joey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-system-dashboard-copy/01-RESEARCH.md
@.planning/phases/01-design-system-dashboard-copy/01-UI-SPEC.md
@.planning/phases/01-design-system-dashboard-copy/01-01-SUMMARY.md

<interfaces>
<!-- app.blade.php estrutura atual (extraída do codebase): -->
<!-- L15: <div class="app-shell"> -->
<!-- L16: <header class="topbar"> -->
<!-- L_main: <main class="wrap page">  (sem flex-1) -->
<!-- L_footer: <footer class="wrap footer-links"> -->

<!-- app.css .app-shell tem DUAS regras: -->
<!-- L64 (@layer components): @apply min-h-screen;  -->
<!-- L186 (fora de layer): min-height: 100vh; padding-top: 76px; -->
<!-- Ambas devem ser atualizadas para incluir flex layout -->

<!-- DashboardController.php estrutura atual: -->
<!-- $missingInventory = collect(['profile'=>!$profile, 'experiences'=>..., 'skills'=>..., 'achievements'=>..., 'languages'=>...]).filter().keys().values() -->
<!-- Atualmente passa: resumeCount, jobCount, latestResume, latestReport, bestReport, missingInventory, latestJobs, latestResumes -->
<!-- Deve ADICIONAR: completeness (int 0-100) -->
<!-- Pode REMOVER: bestReport, latestReport (não mais usados no view após mudança) -->

<!-- dashboard/index.blade.php seção de cards atual (L28-33): -->
<!-- <section class="summary-grid"> com 4 x-ui.metric-card: gerados, analisadas, melhor match, gaps -->
<!-- summary-grid é compartilhado — NÃO modificar a classe CSS -->
<!-- Substituir o section inteiro por <section class="grid grid-cols-1 sm:grid-cols-3 gap-6 mb-6"> -->

<!-- UI-SPEC card de completude (inline — metric-card não tem slot CTA): -->
<!-- <article class="metric-card {{ $completeness >= 80 ? 'metric-card-success' : 'metric-card-warning' }}"> -->
<!--   <p class="metric-label">Completude do inventário</p> -->
<!--   <div class="metric-value">{{ $completeness }}<small>%</small></div> -->
<!--   <p class="metric-meta"> -->
<!--     @if($completeness < 80) -->
<!--       <a class="text-sm font-bold text-blue-400 hover:text-blue-300" href="{{ route('career.index') }}">Complete seu perfil →</a> -->
<!--     @else -->
<!--       <span class="text-emerald-400">Inventário completo ✓</span> -->
<!--     @endif -->
<!--   </p> -->
<!-- </article> -->

<!-- UI-SPEC cards 1 e 2 (usar x-ui.metric-card existente): -->
<!-- Card 1: label="Vagas analisadas" :value="$jobCount" meta link "Ver todas →" jobs.index -->
<!-- Card 2: label="Currículos gerados" :value="$resumeCount" meta link "Ver todos →" resumes.index -->

<!-- Copy strings — welcome.blade.php L21 atual: -->
<!-- 'Currículos personalizados para cada vaga, sem inventar experiências.' -->
<!-- Target: 'Seu currículo certo, para a vaga certa.' -->

<!-- welcome.blade.php L22 sub-headline atual: -->
<!-- 'Cole uma vaga, compare com sua trajetória e gere uma versão do currículo baseada em evidências reais do seu perfil.' -->
<!-- Target: 'Importe seu currículo, analise vagas e gere versões personalizadas que passam pelo ATS.' -->

<!-- lang/pt_BR/messages.php career section: -->
<!-- career.eyebrow: 'Inventário de carreira' (manter) -->
<!-- career.title: 'Fonte da verdade do currículo' → 'Seu inventário de carreira' -->
<!-- career.subtitle: 'Cadastre apenas...' → 'Tudo que você já fez, organizado. A IA usa isso para gerar currículos precisos.' -->

<!-- account/index.blade.php L17 título atual: -->
<!-- 'Access, security and data control' / 'Acesso, segurança e controle de dados' -->
<!-- Target PT-BR: 'Sua conta' -->

<!-- dashboard/index.blade.php x-ui.page-header: -->
<!-- title atual PT-BR: 'Seu centro de comando de currículos' (MANTER — já correto) -->
<!-- subtitle atual PT-BR: 'Analise vagas, veja sinais de aderência e gere currículos focados...' -->
<!-- Target subtitle: 'Analise vagas, gere currículos precisos e acompanhe sua candidatura.' -->

<!-- x-ui.metric-card aceita props: label, value, suffix, meta, tone -->
<!-- tone aceita: 'success' (emerald border/glow), 'warning' (amber border/glow) -->
<!-- NÃO aceita prop CTA — por isso o card de completude é inline -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Footer fixo — flex layout em .app-shell e main</name>
  <files>resources/views/layouts/app.blade.php, resources/css/app.css</files>
  <read_first>
    - resources/views/layouts/app.blade.php (leitura completa — localizar linha com class="app-shell" e a tag main)
    - resources/css/app.css linhas 60–75 (regra @layer .app-shell) e linhas 180–195 (segunda regra .app-shell)
  </read_first>
  <action>
    Editar resources/views/layouts/app.blade.php:
    1. Localizar a div com class="app-shell". Adicionar as classes Tailwind `flex flex-col min-h-screen` diretamente na tag:
       De: class="app-shell"
       Para: class="app-shell flex flex-col min-h-screen"
       (Tailwind classes redundam com CSS mas não conflitam — ok para Tailwind v4)

    2. Localizar a tag main com class="wrap page". Adicionar a classe flex-1:
       De: class="wrap page"
       Para: class="wrap page flex-1"

    Editar resources/css/app.css:
    3. Na regra @layer components .app-shell (linha ~64): substituir `@apply min-h-screen;` por `@apply flex flex-col min-h-screen;`

    4. Na regra fora de layer .app-shell (linha ~186): adicionar `display: flex; flex-direction: column;` à declaração existente:
       De: `.app-shell { min-height: 100vh; padding-top: 76px; }`
       Para: `.app-shell { min-height: 100vh; padding-top: 76px; display: flex; flex-direction: column; }`

    5. Adicionar flex-1 ao .page em app.css se não existir como classe CSS (caso a classe .page não tenha flex-grow, a classe Tailwind flex-1 adicionada ao HTML basta; confirmar após build).

    ATENÇÃO: A regra de print media query (.app-shell linha ~799) define `padding-top: 0 !important` — não remover.
    ATENÇÃO: Não alterar .footer-links em si — apenas o layout wrapper precisa mudar.
  </action>
  <verify>
    <automated>grep -n "flex-1" "/home/joey/Documentos/Personal Projects/vyrko/resources/views/layouts/app.blade.php"</automated>
  </verify>
  <acceptance_criteria>
    - resources/views/layouts/app.blade.php contém `class="wrap page flex-1"` na tag main
    - resources/views/layouts/app.blade.php contém `class="app-shell flex flex-col min-h-screen"` (ou qualquer ordem das classes)
    - resources/css/app.css contém `flex-direction: column` na regra .app-shell fora de @layer (linha ~186)
    - resources/css/app.css @layer components .app-shell contém `flex` no @apply
    - npm run build retorna exit code 0
  </acceptance_criteria>
  <done>Footer gruda no rodapé da viewport em páginas com pouco conteúdo; layout não quebra em páginas com conteúdo longo</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard — 3 cards com $completeness e CTA condicional</name>
  <files>app/Http/Controllers/DashboardController.php, resources/views/dashboard/index.blade.php</files>
  <read_first>
    - app/Http/Controllers/DashboardController.php (leitura completa — entender todas as variáveis passadas ao view)
    - resources/views/dashboard/index.blade.php (leitura completa — entender estrutura atual do section.summary-grid e do @php block)
    - resources/views/components/ui/metric-card.blade.php (verificar props disponíveis: label, value, suffix, meta, tone)
  </read_first>
  <action>
    Editar app/Http/Controllers/DashboardController.php:
    1. Após a linha que define $missingInventory (linha ~21), adicionar:
       $totalSections = 5;
       $completeness = (int) round((($totalSections - $missingInventory->count()) / $totalSections) * 100);

    2. No array retornado ao view(), adicionar 'completeness' => $completeness

    3. Remover 'bestReport' => $bestReport do array (variável não mais usada no view após mudança)
    4. Remover 'latestReport' => do array se existir (não usado no novo view)
    5. Remover a linha $bestReport = ... no topo se bestReport não é mais passado
    6. MANTER: resumeCount, jobCount, missingInventory, latestJobs, latestResumes, latestResume (verificar se latestResume é usado no view)

    Editar resources/views/dashboard/index.blade.php:
    7. Remover o @php block com $bestScore e $missingLabels SE $bestScore não for mais usado (verficar antes de remover)
       - $missingLabels ainda é usado na seção "Próxima melhor ação" — MANTER
       - $bestScore não tem mais uso após remover card "Melhor match" — REMOVER essa linha do @php

    8. Substituir o section.summary-grid (linhas 28–33, os 4 metric-cards) inteiramente por:

       <section class="grid grid-cols-1 sm:grid-cols-3 gap-6 mb-6">
           <x-ui.metric-card
               :label="$en ? 'Analyzed jobs' : 'Vagas analisadas'"
               :value="$jobCount"
               :meta="'<a class=\'text-sm text-blue-400 hover:text-blue-300\' href=\'' . route('jobs.index') . '\'>Ver todas →</a>'"
           />
           <x-ui.metric-card
               :label="$en ? 'Generated resumes' : 'Currículos gerados'"
               :value="$resumeCount"
               :meta="'<a class=\'text-sm text-blue-400 hover:text-blue-300\' href=\'' . route('resumes.index') . '\'>Ver todos →</a>'"
           />
           {{-- Completude: inline porque metric-card não tem slot CTA --}}
           <article class="metric-card {{ $completeness >= 80 ? 'metric-card-success' : 'metric-card-warning' }} min-h-[112px]">
               <p class="metric-label">{{ $en ? 'Inventory completeness' : 'Completude do inventário' }}</p>
               <div class="metric-value">{{ $completeness }}<small>%</small></div>
               <p class="metric-meta">
                   @if ($completeness < 80)
                       <a class="text-sm font-bold text-blue-400 hover:text-blue-300 underline-offset-2 hover:underline"
                          href="{{ route('career.index') }}">Complete seu perfil →</a>
                   @else
                       <span class="text-emerald-400">Inventário completo ✓</span>
                   @endif
               </p>
           </article>
       </section>

    ATENÇÃO: Verificar se metric-card.blade.php renderiza $meta como HTML bruto (via {!! !!}) ou escapado. Se escapado, usar abordagem alternativa para os links: colocar o link diretamente na prop meta via Blade ou usar um slot se disponível. Alternativa segura: adicionar os links "Ver todas/Ver todos" como texto simples na prop meta (sem HTML) — inspecionar o componente antes.

    ATENÇÃO: NÃO modificar nem renomear a classe CSS .summary-grid — outras views dependem dela.
    ATENÇÃO: A seção de "Próxima melhor ação" e as listas latestJobs/latestResumes NÃO devem ser alteradas.
  </action>
  <verify>
    <automated>grep -c "summary-grid" "/home/joey/Documentos/Personal Projects/vyrko/resources/views/dashboard/index.blade.php"</automated>
  </verify>
  <acceptance_criteria>
    - grep retorna 0 para "summary-grid" em dashboard/index.blade.php (section de 4 cards removido)
    - dashboard/index.blade.php contém `grid grid-cols-1 sm:grid-cols-3 gap-6`
    - dashboard/index.blade.php contém `Completude do inventário`
    - dashboard/index.blade.php contém `$completeness < 80` (lógica CTA condicional)
    - dashboard/index.blade.php contém `Complete seu perfil →`
    - DashboardController.php contém `$completeness = (int) round(`
    - DashboardController.php contém `'completeness' => $completeness`
    - DashboardController.php NÃO contém `'bestReport' => $bestReport` (removido)
    - php artisan route:list retorna exit code 0 (sem erros de sintaxe PHP)
  </acceptance_criteria>
  <done>Dashboard exibe exatamente 3 cards; $completeness calculado no controller; CTA "Complete seu perfil →" visível quando completeness < 80%</done>
</task>

<task type="auto">
  <name>Task 3: Copy strings — landing, inventário, conta, dashboard subtitle</name>
  <files>resources/views/welcome.blade.php, lang/pt_BR/messages.php, resources/views/account/index.blade.php, resources/views/dashboard/index.blade.php</files>
  <read_first>
    - resources/views/welcome.blade.php linhas 18–30 (section#home com h1 e sub-headline)
    - lang/pt_BR/messages.php (leitura completa — ver chaves career.title, career.subtitle, career.eyebrow)
    - resources/views/account/index.blade.php linhas 14–22 (x-ui.page-header com título)
    - resources/views/dashboard/index.blade.php linhas 17–26 (x-ui.page-header com subtitle)
  </read_first>
  <action>
    Editar resources/views/welcome.blade.php:
    1. Localizar o h1 dentro da section#home. Alterar o texto PT-BR:
       De: 'Currículos personalizados para cada vaga, sem inventar experiências.'
       Para: 'Seu currículo certo, para a vaga certa.'

    2. Alterar o parágrafo PT-BR imediatamente após o h1 (sub-headline):
       De: 'Cole uma vaga, compare com sua trajetória e gere uma versão do currículo baseada em evidências reais do seu perfil.'
       Para: 'Importe seu currículo, analise vagas e gere versões personalizadas que passam pelo ATS.'

    Editar lang/pt_BR/messages.php:
    3. career.title: alterar de 'Fonte da verdade do currículo' para 'Seu inventário de carreira'
    4. career.subtitle: alterar de 'Cadastre apenas informações reais. Currículos gerados usam evidências deste inventário.' para 'Tudo que você já fez, organizado. A IA usa isso para gerar currículos precisos.'
    5. career.eyebrow: manter 'Inventário de carreira' (já correto)

    Editar resources/views/account/index.blade.php:
    6. No x-ui.page-header, alterar o :title para PT-BR:
       De: "$en ? 'Access, security and data control' : 'Acesso, segurança e controle de dados'"
       Para: "$en ? 'Your account' : 'Sua conta'"

    Editar resources/views/dashboard/index.blade.php:
    7. No x-ui.page-header, verificar o :subtitle PT-BR atual e alterar para:
       De: 'Analise vagas, veja sinais de aderência e gere currículos focados a partir de evidências reais da carreira.'
       Para: 'Analise vagas, gere currículos precisos e acompanhe sua candidatura.'
    8. O :title PT-BR 'Seu centro de comando de currículos' já está correto — MANTER.

    ATENÇÃO: As strings EN em welcome.blade.php NÃO devem ser alteradas (pares bilíngues com $en ternário).
    ATENÇÃO: account/index.blade.php usa :title como prop, não como chave de tradução — alterar diretamente na view.
    ATENÇÃO: Verificar se career/index.blade.php usa __('messages.career.title') ou string hardcoded para saber se a mudança em messages.php propaga automaticamente.
  </action>
  <verify>
    <automated>grep -n "Seu currículo certo" "/home/joey/Documentos/Personal Projects/vyrko/resources/views/welcome.blade.php"</automated>
  </verify>
  <acceptance_criteria>
    - welcome.blade.php contém 'Seu currículo certo, para a vaga certa.' como string PT-BR do h1
    - welcome.blade.php contém 'Importe seu currículo, analise vagas e gere versões personalizadas que passam pelo ATS.' como sub-headline PT-BR
    - lang/pt_BR/messages.php career.title = 'Seu inventário de carreira'
    - lang/pt_BR/messages.php career.subtitle = 'Tudo que você já fez, organizado. A IA usa isso para gerar currículos precisos.'
    - account/index.blade.php contém 'Sua conta' como título PT-BR
    - dashboard/index.blade.php subtitle PT-BR = 'Analise vagas, gere currículos precisos e acompanhe sua candidatura.'
    - php artisan route:list retorna exit code 0 (sem erros de sintaxe em nenhum arquivo Blade)
  </acceptance_criteria>
  <done>Landing h1 PT-BR correto; inventário com novo título/subtítulo; conta mostra "Sua conta"; dashboard com subtítulo atualizado</done>
</task>

<task type="auto">
  <name>Task 4: Copy COPY-02 gap — heading de Templates e empty states orientadores</name>
  <files>resources/views/resumes/templates.blade.php, resources/views/dashboard/index.blade.php, resources/views/jobs/index.blade.php, resources/views/resumes/index.blade.php</files>
  <read_first>
    - resources/views/resumes/templates.blade.php (leitura completa — localizar o heading principal da página, identificar onde atualizar para "Qual currículo enviar para esta vaga?")
    - resources/views/dashboard/index.blade.php (localizar x-ui.empty-state ou estrutura de empty state para vagas e currículos; pode ser um @if($jobCount == 0) block ou componente)
    - resources/views/jobs/index.blade.php (localizar empty state da lista de vagas — heading, body, CTA)
    - resources/views/resumes/index.blade.php (localizar empty state da lista de currículos — heading, body, CTA)

    ATENÇÃO: Se os arquivos jobs/index.blade.php ou resumes/index.blade.php não existem no caminho esperado, verificar resources/views/jobs/ e resources/views/resumes/ com ls antes de editar.
  </read_first>
  <action>
    Editar resources/views/resumes/templates.blade.php:
    1. Localizar o heading principal da página (h1, h2, ou prop :title de um x-ui.page-header). Alterar o texto PT-BR para:
       "Qual currículo enviar para esta vaga?"
       Manter o equivalente EN se existir como par ternário com $en.

    Editar resources/views/dashboard/index.blade.php — Empty states do dashboard:
    2. Localizar o empty state exibido quando não há vagas analisadas ($jobCount == 0 ou similar). Atualizar as três strings:
       - Heading PT-BR: "Nenhuma vaga analisada ainda"
       - Body PT-BR: "Cole a descrição de uma vaga e a IA mapeia seu match."
       - CTA PT-BR: "Analisar primeira vaga →"

    3. Localizar o empty state exibido quando não há currículos gerados ($resumeCount == 0 ou similar). Atualizar as três strings:
       - Heading PT-BR: "Nenhum currículo gerado ainda"
       - Body PT-BR: "Analise uma vaga para gerar seu currículo personalizado."
       - CTA PT-BR: "Começar por uma vaga →"

    Editar resources/views/jobs/index.blade.php — Empty state da lista de vagas:
    4. Localizar o bloco de empty state (quando não há jobs cadastrados). Atualizar:
       - Heading PT-BR: "Nenhum workspace ainda"
       - Body PT-BR: "Cada análise de vaga vira um workspace com match e currículo."
       - CTA PT-BR: "Analisar nova vaga"

    Editar resources/views/resumes/index.blade.php — Empty state da lista de currículos:
    5. Localizar o bloco de empty state (quando não há currículos para a vaga). Atualizar:
       - Heading PT-BR: "Nenhum currículo nesta vaga"
       - Body PT-BR: "Gere o currículo certo para esta vaga em segundos."
       - CTA PT-BR: "Gerar currículo →"

    ATENÇÃO: Os empty states podem ser renderizados via componente x-ui.empty-state (com props :heading, :body, :cta ou similares) ou via HTML inline com @if. Ler o arquivo antes de editar para determinar a estrutura exata.
    ATENÇÃO: Manter strings EN intactas se existirem em pares ternários com $en. Alterar apenas o ramo PT-BR.
    ATENÇÃO: Se um empty state não existir na view (ex: jobs/index apenas mostra lista sem empty state), criar um bloco @if($jobs->isEmpty()) com as strings especificadas, seguindo o padrão de outros empty states no projeto.
    ATENÇÃO: O heading "Nenhuma vaga analisada ainda" é diferente entre dashboard ("Nenhuma vaga analisada ainda") e lista de vagas ("Nenhum workspace ainda") — aplicar cada string ao local correto.
  </action>
  <verify>
    <automated>grep -rn "Qual currículo enviar para esta vaga" "/home/joey/Documentos/Personal Projects/vyrko/resources/views/resumes/templates.blade.php" && grep -rn "Nenhuma vaga analisada ainda" "/home/joey/Documentos/Personal Projects/vyrko/resources/views/dashboard/index.blade.php" && grep -rn "Nenhum currículo gerado ainda" "/home/joey/Documentos/Personal Projects/vyrko/resources/views/dashboard/index.blade.php" && grep -rn "Nenhum workspace ainda" "/home/joey/Documentos/Personal Projects/vyrko/resources/views/jobs/index.blade.php" && grep -rn "Nenhum currículo nesta vaga" "/home/joey/Documentos/Personal Projects/vyrko/resources/views/resumes/index.blade.php"</automated>
  </verify>
  <acceptance_criteria>
    - resources/views/resumes/templates.blade.php contém a string "Qual currículo enviar para esta vaga?"
    - resources/views/dashboard/index.blade.php contém "Nenhuma vaga analisada ainda" no bloco de empty state de vagas
    - resources/views/dashboard/index.blade.php contém "Cole a descrição de uma vaga e a IA mapeia seu match."
    - resources/views/dashboard/index.blade.php contém "Analisar primeira vaga →"
    - resources/views/dashboard/index.blade.php contém "Nenhum currículo gerado ainda" no bloco de empty state de currículos
    - resources/views/dashboard/index.blade.php contém "Analise uma vaga para gerar seu currículo personalizado."
    - resources/views/dashboard/index.blade.php contém "Começar por uma vaga →"
    - resources/views/jobs/index.blade.php contém "Nenhum workspace ainda"
    - resources/views/jobs/index.blade.php contém "Cada análise de vaga vira um workspace com match e currículo."
    - resources/views/jobs/index.blade.php contém "Analisar nova vaga"
    - resources/views/resumes/index.blade.php contém "Nenhum currículo nesta vaga"
    - resources/views/resumes/index.blade.php contém "Gere o currículo certo para esta vaga em segundos."
    - resources/views/resumes/index.blade.php contém "Gerar currículo →"
    - php artisan route:list retorna exit code 0 (sem erros de sintaxe Blade)
  </acceptance_criteria>
  <done>Página de templates com heading correto; 4 empty states (dashboard vagas, dashboard currículos, lista vagas, lista currículos) com strings orientadoras da UI-SPEC</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DashboardController → view | Variável $completeness calculada server-side — sem input de usuário |
| Blade views → HTML output | Strings de copy são estáticas — sem risco de XSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | metric-card meta prop com HTML | mitigate | Verificar se metric-card usa {!! !!} (raw) ou {{ }} (escaped) antes de passar HTML na prop — se escaped, usar texto plano ou Blade slot em vez de HTML inline na prop |
| T-02-02 | Denial of Service | $missingInventory->count() = 5 (divisão) | accept | Denominador é constante 5 — sem risco de divisão por zero |
| T-02-SC | Tampering | npm/pip installs | accept | Plano 02 não instala novos pacotes |
</threat_model>

<verification>
Após execução deste plano:

1. `grep -n "flex-1" resources/views/layouts/app.blade.php` retorna match na tag main
2. `grep -c "summary-grid" resources/views/dashboard/index.blade.php` retorna 0
3. `grep "completeness" app/Http/Controllers/DashboardController.php` retorna matches para cálculo e passagem ao view
4. `grep "Seu currículo certo" resources/views/welcome.blade.php` retorna match
5. `grep "Seu inventário de carreira" lang/pt_BR/messages.php` retorna match
6. `grep "Sua conta" resources/views/account/index.blade.php` retorna match
7. `grep "Qual currículo enviar para esta vaga" resources/views/resumes/templates.blade.php` retorna match
8. `grep "Nenhuma vaga analisada ainda" resources/views/dashboard/index.blade.php` retorna match
9. `grep "Nenhum workspace ainda" resources/views/jobs/index.blade.php` retorna match
10. `grep "Nenhum currículo nesta vaga" resources/views/resumes/index.blade.php` retorna match
11. `npm run build` retorna exit code 0
</verification>

<success_criteria>
- DESIGN-02: Footer fixo no rodapé em todas as páginas (flex layout aplicado)
- DASH-01: Dashboard com exatamente 3 cards (Vagas analisadas, Currículos gerados, Completude)
- DASH-02: Card de completude com CTA "Complete seu perfil →" quando < 80%
- COPY-01: Landing exibe "Seu currículo certo, para a vaga certa."
- COPY-02: Inventário "Seu inventário de carreira"; Templates "Qual currículo enviar para esta vaga?"; Conta "Sua conta"; dashboard subtitle atualizado; 4 empty states com strings orientadoras corretas
</success_criteria>

<output>
Criar `.planning/phases/01-design-system-dashboard-copy/01-02-SUMMARY.md` quando concluído
</output>
