---
phase: 02-invent-rio-com-import
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - app/Services/Ai/GeminiAiClient.php
  - resources/views/career/partials/inventory-list.blade.php
  - tests/Feature/VyrkoMvpTest.php
autonomous: true
requirements: [INV-03]

must_haves:
  truths:
    - "O prompt de IA define sub-schemas tipados para educations, certifications, languages e projects"
    - "O campo 'proficiency' de languages tem valores enumerados explícitos no schema"
    - "O campo 'issuer' de certifications está no schema e mapeado ao persist correspondente"
    - "Itens importados pela IA exibem badge 'via IA' na listagem do inventário"
    - "Badge usa flash session do Laravel — não localStorage"
  artifacts:
    - path: "app/Services/Ai/GeminiAiClient.php"
      provides: "resumeImportParsePrompt com sub-schemas completos para todos os arrays"
      contains: "institution"
    - path: "resources/views/career/partials/inventory-list.blade.php"
      provides: "Badge 'via IA' renderizado nos items cujos IDs estão na flash session import_badge_ids"
      contains: "via IA"
    - path: "tests/Feature/VyrkoMvpTest.php"
      provides: "Teste que verifica campos extraídos pelo prompt incluem proficiency e issuer"
      contains: "test_importacao_mapeia_campos_campo_a_campo"
  key_links:
    - from: "GeminiAiClient::resumeImportParsePrompt"
      to: "ResumeImportService::persistEducations / persistCertifications / persistLanguages"
      via: "schema tipado → Gemini retorna campos corretos → persist usa os campos"
      pattern: "institution.*degree|issuer.*issued_at|language.*proficiency"
    - from: "ResumeImportController"
      to: "inventory-list.blade.php"
      via: "session(['import_badge_ids' => $ids]) → Blade checa session('import_badge_ids')"
      pattern: "import_badge_ids"
---

<objective>
Revisar o prompt de IA com sub-schemas tipados para os 4 arrays vagos e adicionar badge "via IA" nos itens importados via flash session.

Purpose: INV-03 — o prompt atual tem 'educations': ['array'], 'certifications': ['array'], etc. sem sub-schema. Gemini não sabe os campos esperados e pode inventar ou omitir dados. Os sub-schemas tipados forçam extração campo a campo sem inferência genérica.
Output: resumeImportParsePrompt com sub-schemas completos; badge "via IA" em inventory-list.blade.php renderizado via flash session.
</objective>

<execution_context>
@/home/joey/.claude/get-shit-done/workflows/execute-plan.md
@/home/joey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-invent-rio-com-import/02-RESEARCH.md
@.planning/phases/02-invent-rio-com-import/02-UI-SPEC.md
@.planning/phases/02-invent-rio-com-import/02-01-SUMMARY.md

<interfaces>
<!-- Prompt atual em GeminiAiClient.php (linhas 250-286): -->
<!-- Os 4 arrays vagos: -->
<!--   'projects' => ['array'],       # deve virar array de objetos tipados -->
<!--   'educations' => ['array'],     # deve virar array com institution/degree/field_of_study/start_date/end_date -->
<!--   'certifications' => ['array'], # deve virar array com name/issuer/issued_at/expires_at/credential_url -->
<!--   'languages' => ['array'],      # deve virar array com language/proficiency/notes -->

<!-- persistEducations espera (verificar no ResumeImportService): institution, degree, field_of_study, start_date, end_date -->
<!-- persistCertifications espera: name, issuer, issued_at, expires_at, credential_url -->
<!-- persistLanguages espera: language, proficiency (nível), notes -->
<!-- persistProjects espera: name, description, url, start_date, end_date, technologies -->

<!-- Flash session para badge: -->
<!-- O controller ResumeImportController::__invoke() recebe retorno do service -->
<!-- O service retorna (ou pode retornar) os IDs dos itens criados -->
<!-- Adicionar ao controller: session(['import_badge_ids' => $service->getLastImportIds()]) ou similar -->
<!-- Na view inventory-list.blade.php, checar session('import_badge_ids') para cada item -->

<!-- Badge visual (UI-SPEC): -->
<!-- span: "via IA" — 11px, peso 700, text-blue-300, bg-blue-500/10, border border-blue-500/20, -->
<!--        rounded-full, px-2 py-0.5, uppercase, letter-spacing: 0.06em -->
<!-- aria-label="Preenchido pela IA durante importação" -->
<!-- Posição: dentro do .career-item-meta ou do header do career-item -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teste Wave 0 + leitura do ResumeImportService</name>
  <files>tests/Feature/VyrkoMvpTest.php</files>
  <read_first>
    - app/Services/Import/ResumeImportService.php (completo — ver persistEducations, persistCertifications, persistLanguages, persistProjects e quais campos cada método usa; ver se retorna IDs)
    - app/Http/Controllers/Import/ResumeImportController.php (ver como chama o service e o que faz com o retorno)
    - tests/Feature/VyrkoMvpTest.php (linhas 215-244 — ver test_importacao_de_curriculo_txt_absorve_informacoes existente como referência de padrão)
  </read_first>
  <behavior>
    - test_importacao_mapeia_campos_campo_a_campo: após import de TXT simulado com FakeAiClient que retorna JSON com education[institution="UFMG", degree="Bacharelado"], certification[name="AWS SAA", issuer="Amazon", issued_at="2023-01-01"], language[language="Inglês", proficiency="Avançado"] → assertDatabaseHas('candidate_educations', ['institution' => 'UFMG']) + assertDatabaseHas('candidate_certifications', ['issuer' => 'Amazon']) + assertDatabaseHas('candidate_languages', ['proficiency' => 'Avançado'])
  </behavior>
  <action>
    Ler ResumeImportService.php completo para entender:
    1. Quais campos cada persist* método salva no banco (institution, degree, issuer, proficiency, etc.)
    2. Se o service retorna os IDs criados nessa importação (para o badge)
    3. Se ResumeImportController captura esse retorno

    Depois ler VyrkoMvpTest.php linhas 215-244 para entender o padrão do FakeAiClient/MockAiClient.

    Adicionar o método test_importacao_mapeia_campos_campo_a_campo() ao VyrkoMvpTest seguindo o mesmo padrão do teste existente:
    - Criar usuário autenticado
    - Criar UploadedFile fake com conteúdo TXT que inclua dados de educação/certificação/idioma
    - O FakeAiClient (ou mock configurado no teste) deve retornar JSON com os sub-schemas que o Plan 02-03 Task 2 vai implementar
    - POST route('career.import') com o arquivo
    - assertDatabaseHas para os 3 tabelas: candidate_educations (institution), candidate_certifications (issuer), candidate_languages (proficiency)

    Se o projeto usa FakeAiClient injetado via config/service container no ambiente de teste — usar o mesmo mecanismo do teste existente.
    Se o projeto usa mocking direto via $this->mock() — usar o mesmo padrão.

    O teste deve falhar (RED) com "Expected row not found" antes das alterações do Task 2.
  </action>
  <verify>
    <automated>cd "/home/joey/Documentos/Personal Projects/vyrko" && php artisan test --filter=test_importacao_mapeia_campos_campo_a_campo 2>&1 | tail -8</automated>
  </verify>
  <done>Teste existe e falha com assertion failure (não erro de sintaxe) — RED confirmado. Ou PASS se o schema atual já mapeia corretamente (nesse caso pular Task 2 para os campos já cobertos).</done>
</task>

<task type="auto">
  <name>Task 2: Sub-schemas tipados no resumeImportParsePrompt + badge "via IA" no Blade</name>
  <files>app/Services/Ai/GeminiAiClient.php, resources/views/career/partials/inventory-list.blade.php</files>
  <read_first>
    - app/Services/Ai/GeminiAiClient.php (linhas 248-295 — função resumeImportParsePrompt completa)
    - app/Services/Import/ResumeImportService.php (métodos persist* — confirmar nomes dos campos esperados)
    - resources/views/career/partials/inventory-list.blade.php (estrutura de .career-item para saber onde inserir badge)
    - app/Http/Controllers/Import/ResumeImportController.php (ver se já expõe IDs para flash session)
  </read_first>
  <action>
    PARTE 1 — Substituir os 4 arrays vagos no prompt (GeminiAiClient.php):

    Localizar em `resumeImportParsePrompt()` as 4 linhas:
    ```
    'projects' => ['array'],
    'educations' => ['array'],
    'certifications' => ['array'],
    'languages' => ['array'],
    ```

    Substituir por sub-schemas tipados (arrays de objetos com campos nomeados):

    'educations' deve virar:
    ```php
    'educations' => [
        ['institution' => 'string', 'degree' => 'string|null', 'field_of_study' => 'string|null', 'start_date' => 'YYYY-MM-DD|null', 'end_date' => 'YYYY-MM-DD|null'],
    ],
    ```

    'certifications' deve virar:
    ```php
    'certifications' => [
        ['name' => 'string', 'issuer' => 'string|null', 'issued_at' => 'YYYY-MM-DD|null', 'expires_at' => 'YYYY-MM-DD|null', 'credential_url' => 'string|null'],
    ],
    ```

    'languages' deve virar:
    ```php
    'languages' => [
        ['language' => 'string', 'proficiency' => 'Nativo|Fluente|Avançado|Intermediário|Básico', 'notes' => 'string|null'],
    ],
    ```

    'projects' deve virar:
    ```php
    'projects' => [
        ['name' => 'string', 'description' => 'string|null', 'url' => 'string|null', 'start_date' => 'YYYY-MM-DD|null', 'end_date' => 'YYYY-MM-DD|null', 'technologies' => ['string']],
    ],
    ```

    VERIFICAR antes de escrever: confirmar com leitura do ResumeImportService se os nomes dos campos correspondem exatamente ao que os métodos persist* usam. Se houver discrepância, usar os nomes que o service espera (o service é a fonte de verdade).

    TAMBÉM adicionar instrução anti-inferência mais forte no texto do prompt (primeiro argumento do jsonPrompt):
    Atual: "Extraia dados estruturados do currículo enviado. Não complete lacunas por inferência; se o texto não disser, use null ou array vazio."
    Novo: "Extraia dados estruturados do currículo enviado. Regras estritas: (1) Não complete lacunas por inferência — se o texto não disser explicitamente, use null ou array vazio. (2) Não invente dados de contato, datas ou empresas. (3) Não combine campos de seções diferentes. (4) Cada item de array representa exatamente um registro encontrado no texto."

    PARTE 2 — Badge "via IA" em inventory-list.blade.php:

    Ler o controller ResumeImportController para entender se ele expõe IDs dos itens criados.
    Ler ResumeImportService para ver se retorna IDs.

    Se o service já retorna IDs (ou pode ser adaptado com mudança mínima de 1-2 linhas):
    - Adicionar ao controller após a chamada do service: `session()->flash('import_badge_ids', $importedIds)` onde $importedIds é array de IDs
    - Em inventory-list.blade.php, em cada `.career-item` de seções relevantes (experiences, educations, certifications, languages, skills), verificar se o ID do item está em `session('import_badge_ids', [])`:
      ```blade
      @if(in_array($item->id, session('import_badge_ids', [])))
          <span class="import-badge" aria-label="Preenchido pela IA durante importação">VIA IA</span>
      @endif
      ```
    - Adicionar CSS para `.import-badge` em @push('styles') de index.blade.php (ou inline na view):
      `font-size:11px; font-weight:700; color:#93C5FD; background:rgba(59,130,246,.1); border:1px solid rgba(59,130,246,.2); border-radius:999px; padding:2px 8px; text-transform:uppercase; letter-spacing:.06em;`

    Se o service NÃO retorna IDs e a mudança seria > 10 linhas de backend:
    - Usar abordagem de flash boolean: `session()->flash('just_imported', true)`
    - No Blade, não filtrar por ID mas mostrar badge em TODOS os items de sessões que foram importadas (experiences, educations, certifications, languages)
    - Isso é menos preciso mas não requer mudanças no service

    Escolher a abordagem mais simples que entrega o comportamento visual especificado no UI-SPEC.
  </action>
  <verify>
    <automated>cd "/home/joey/Documentos/Personal Projects/vyrko" && php artisan test --filter=test_importacao_mapeia_campos_campo_a_campo 2>&1 | tail -8</automated>
  </verify>
  <done>
    - grep "institution" app/Services/Ai/GeminiAiClient.php retorna >= 1
    - grep "issuer" app/Services/Ai/GeminiAiClient.php retorna >= 1
    - grep "proficiency" app/Services/Ai/GeminiAiClient.php retorna >= 1 (no schema de languages)
    - grep "via IA\|VIA IA\|import-badge" resources/views/career/partials/inventory-list.blade.php retorna >= 1
    - php artisan test --filter=test_importacao_mapeia_campos_campo_a_campo → PASS (se FakeAiClient retornar os campos novos)
    - php artisan test --no-coverage → suite completa verde
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini API → ResumeImportService | Resposta JSON da IA parseada via json_decode — conteúdo não executado, apenas persistido |
| Flash session import_badge_ids | Gerado pelo servidor, nunca por input do usuário — sem risco de injeção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | Prompt inclui resume_text do usuário enviado à Gemini API | accept | Dado já enviado à Gemini em todas as importações existentes; nenhuma mudança nesta fase aumenta o escopo de disclosure |
| T-02-07 | Tampering | Sub-schemas tipados forçam Gemini a retornar campos específicos | accept | Gemini pode ignorar o schema — persistência é defensiva (updateOrCreate com campos conhecidos); campos desconhecidos são descartados pelo service |
| T-02-08 | Spoofing | Badge "via IA" via flash session | accept | Flash session é server-side e expira na próxima request — não pode ser forjado pelo cliente |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado neste plano |
</threat_model>

<verification>
php artisan test --filter=ImportCard
→ 2 testes PASS (regressão do Plan 01)

php artisan test --filter=test_importacao_mapeia_campos_campo_a_campo
→ PASS

php artisan test --no-coverage
→ suite completa verde

grep -c "institution" app/Services/Ai/GeminiAiClient.php
→ >= 1

grep -c "import-badge\|via IA\|VIA IA" resources/views/career/partials/inventory-list.blade.php
→ >= 1
</verification>

<success_criteria>
- resumeImportParsePrompt define sub-schemas para educations (institution/degree/field_of_study/start_date/end_date), certifications (name/issuer/issued_at/expires_at/credential_url), languages (language/proficiency enum/notes), projects (name/description/url/start_date/end_date/technologies)
- Instrução anti-inferência reforçada com 4 regras explícitas
- Badge "via IA" renderizado em inventory-list.blade.php para itens da sessão de importação
- php artisan test --no-coverage verde sem regressões
</success_criteria>

<output>
Criar `.planning/phases/02-invent-rio-com-import/02-03-SUMMARY.md` ao concluir
</output>
